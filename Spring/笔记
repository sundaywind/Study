1.获取Spring核心容器的两种方式

2.从Spring核心容器获取Bean对象的两种方式

3.Bean对象的创建时间、创建的Bean对象是单利还是多利（如果就想把Bean对象的创建变为多利的？怎么做）、Bean对象的生命周期

4.IOC、DI

5.Spring容器创建Bean对象的三种方式

6.分模块开发

7.工厂模式：工厂都是创建对象的！

8.通过类字节码和id获取bean对象的区别：配置里有相同class属性的类，用字节码是会报错的，因为返回的对象是一个，但找到了两个。

9.属性注入的四种方式（给类的属性的赋值）：set方法注入、构造器赋值注入、P名称空间注入、SPEL表达式。